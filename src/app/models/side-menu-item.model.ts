export class SideMenuItem {
    icon: string;
    name: string;
    route: string;

    constructor(icon: string, name: string, route: string) {
        this.icon = icon;
        this.name = name;
        this.route = route;
    }
}